---
title: 'Dropdown'
description: 'Dropdowns present selectable lists with optional search, multi-select, and formatter support.'
---

import DropdownPlayground from '@/components/demo/playground/Dropdown';
import { TypeTable } from '@/components/type-table';

## Playground

<DropdownPlayground />

## Usage

You can create a Dropdown by calling the `AddDropdown` method on a Groupbox.

### Arguments

```lua
local Dropdown = Groupbox:AddDropdown("MyDropdown", {
    Text = "A dropdown",
    Values = { "This", "is", "a", "dropdown" },
    Default = 1,
    Multi = false,
})
```

| Arg Idx | Argument Description | Type | Default |
| --- | --- | --- | --- |
| 1 | Index/ID of the dropdown | string | nil |
| 2 | Dropdown configuration table | table | nil |

### Configuration Table

<TypeTable
  type={{
    Text: {
      description: "The dropdown's label text",
      type: 'string',
      default: 'nil',
      required: true
    },
    Values: {
      description: 'Selectable entries. Array: each entry is the selection identity and display text. Dictionary: key = selection identity, value = display label. Sparse numeric tables are treated as arrays (value identity), not dictionaries.',
      type: 'table',
      default: '{}',
      required: true
    },
    Default: {
      description: 'Default selection: array index, identity string, or table of identities. For dictionary Values, use a key (or table of keys), not the display label.',
      type: 'any',
      default: 'nil',
      required: false
    },
    Multi: {
      description: 'Whether multiple values can be selected',
      type: 'boolean',
      default: 'false',
      required: false
    },
    DragSelect: {
      description: 'Enables dragging to select multiple items (only works on non-touch devices and Multi dropdowns)',
      type: 'boolean',
      default: 'false',
      required: false
    },
    AllowNull: {
      description: 'Whether no selection is allowed',
      type: 'boolean',
      default: 'false',
      required: false
    },
    Searchable: {
      description: 'Enables searching through list labels.',
      type: 'boolean',
      default: 'false',
      required: false
    },
    MaxVisibleDropdownItems: {
      description: 'Maximum number of visible rows in the dropdown menu (caps height). Large lists are virtualized with a fixed row pool so instance count stays roughly constant.',
      type: 'number',
      default: '8',
      required: false
    },
    SpecialType: {
      description: 'Special data source ("Player" or "Team")',
      type: 'string',
      default: 'nil',
      required: false
    },
    ExcludeLocalPlayer: {
      description: 'For Player type, excludes the local player',
      type: 'boolean',
      default: 'false',
      required: false
    },
    EnablePlayerImages: {
      description: 'For Player type, enables player avatar headshots',
      type: 'boolean',
      default: 'false',
      required: false
    },
    FormatDisplayValue: {
      description: 'Function to format displayed values',
      type: 'function',
      default: 'nil',
      required: false
    },
    FormatListValue: {
      description: 'Function to format values inside the context menu list. Receives the raw Values entry (array value, or dictionary label). Randomized formatting will not be preserved as the function is called whenever the list is rebuilt.',
      type: 'function',
      default: 'nil',
      required: false
    },
    DisabledValues: {
      description: 'Identities that cannot be selected. For dictionary Values, entries may be keys or display labels.',
      type: 'table',
      default: '{}',
      required: false
    },
    ValueImages: {
      description: 'Maps selection identities (or dictionary labels) to images',
      type: 'table',
      default: '{}',
      required: false
    },
    Tooltip: {
      description: 'Tooltip text shown on hover',
      type: 'string',
      default: 'nil',
      required: false
    },
    DisabledTooltip: {
      description: 'Tooltip shown when disabled',
      type: 'string',
      default: 'nil',
      required: false
    },
    Callback: {
      description: 'Called when the selection changes',
      type: 'function',
      default: 'function() end',
      required: false
    },
    Disabled: {
      description: 'Whether the dropdown is disabled',
      type: 'boolean',
      default: 'false',
      required: false
    },
    Visible: {
      description: 'Whether the dropdown is visible',
      type: 'boolean',
      default: 'true',
      required: false
    }
  }}
/>

## Values

`Values` can be a normal list or a dictionary.

### List

Each entry is both what you select and what the user sees:

```lua
Values = { "Sword", "Shield" }
-- selecting Sword -> Value is "Sword"
```

### Dictionary

The key is what your script gets back; the label is what the user sees:

```lua
Values = { item01 = "Excalibur", item05 = "Aegis Shield" }
-- selecting Excalibur -> Value is "item01" (not "Excalibur")
```

Use a dictionary when the UI needs friendly names but your code needs stable IDs (or other data). Look up the label or metadata with `Values[selectedKey]`.

For multi dropdowns, `Value` is always a map of selected keys to `true` (for example `{ item01 = true }`).

`Default`, `SetValue`, and `OnChanged` use the **key** for dictionaries, not the label. See the [Example](#example) below.

## Methods

You can use the following methods to interact with the dropdown:

#### SetValue
Sets the selected value or values. For dictionary dropdowns, pass keys (or `{ [key] = true }` for multi), not display labels.

```lua
Dropdown:SetValue(value)
```

| Arg Idx | Argument Description | Type | Default |
| --- | --- | --- | --- |
| 1 | Value to select (string, number, or table) | any | nil |

#### SetValues
Replaces all dropdown values (array or dictionary). Selections that are no longer present are pruned; if anything was removed, `Callback` may fire.

```lua
Dropdown:SetValues(table)
```

| Arg Idx | Argument Description | Type | Default |
| --- | --- | --- | --- |
| 1 | New values list or dictionary map | table | nil |

#### AddValues
Adds new values to the dropdown.

- **Array** `Values`: appends entries (`table.insert`).
- **Dictionary** `Values`: merges maps (`{ key = label }`). Array-style tables become key=label pairs (`{ "X" }` → `Values.X = "X"`).

```lua
Dropdown:AddValues(value)
```

| Arg Idx | Argument Description | Type | Default |
| --- | --- | --- | --- |
| 1 | Value, array of values, or dictionary map to merge | any | nil |

#### SetDisabledValues
Sets which values are disabled. For dictionary dropdowns, entries may be keys or labels.

```lua
Dropdown:SetDisabledValues(table)
```

| Arg Idx | Argument Description | Type | Default |
| --- | --- | --- | --- |
| 1 | Table of values to disable | table | nil |

#### AddDisabledValues
Adds values to the disabled list.

```lua
Dropdown:AddDisabledValues(value)
```

| Arg Idx | Argument Description | Type | Default |
| --- | --- | --- | --- |
| 1 | Value or table of values to disable | any | nil |

#### SetValueImages
Replaces value to image mapping. For dictionary dropdowns, keys may be selection identities or labels.

```lua
Dropdown:SetValueImages(table)
```

| Arg Idx | Argument Description | Type | Default |
| --- | --- | --- | --- |
| 1 | Table mapping values to images | table | nil |

#### AddValueImages
Adds new value to image mapping into the existing ones.

```lua
Dropdown:AddValueImages(table)
```

| Arg Idx | Argument Description | Type | Default |
| --- | --- | --- | --- |
| 1 | Table mapping values to images to add | table | nil |

#### SetText
Updates the dropdown's label text.

```lua
Dropdown:SetText(text)
```

| Arg Idx | Argument Description | Type | Default |
| --- | --- | --- | --- |
| 1 | New label text | string | nil |

#### SetDisabled
Enables or disables the dropdown.

```lua
Dropdown:SetDisabled(boolean)
```

| Arg Idx | Argument Description | Type | Default |
| --- | --- | --- | --- |
| 1 | Whether to disable the dropdown | boolean | nil |

#### SetVisible
Shows or hides the dropdown.

```lua
Dropdown:SetVisible(boolean)
```

| Arg Idx | Argument Description | Type | Default |
| --- | --- | --- | --- |
| 1 | Whether to show the dropdown | boolean | nil |

#### SetDragSelect
Enables or disables dragging to select multiple items.

```lua
Dropdown:SetDragSelect(boolean)
```

| Arg Idx | Argument Description | Type | Default |
| --- | --- | --- | --- |
| 1 | Whether to enable drag select | boolean | nil |

#### GetActiveValues
Returns the currently selected values (selection values or keys).

```lua
local values = Dropdown:GetActiveValues()
local count = Dropdown:GetActiveValues(true)
```

| Arg Idx | Argument Description | Type | Default |
| --- | --- | --- | --- |
| 1 | Return count for multi dropdowns | boolean? | false |

#### Destroy
Destroys the dropdown and removes it from the UI.

```lua
Dropdown:Destroy()
```

#### OnChanged
Adds another callback function.

```lua
Dropdown:OnChanged(function)
```

| Arg Idx | Argument Description | Type | Default |
| --- | --- | --- | --- |
| 1 | Callback executed when the dropdown changes | function | nil |

## Example

```lua
local Options = Library.Options

local Dropdown = Groupbox:AddDropdown("MyDropdown", {
    Text = "A dropdown",
    Values = { "This", "is", "a", "dropdown" },
    Default = 1,
})

Options.MyDropdown:OnChanged(function()
    print("Selected value:", Options.MyDropdown.Value)
end)

Options.MyDropdown:SetValue("This")

-- Dictionary Values: key = identity, value = label
Groupbox:AddDropdown("MyDictionaryDropdown", {
    Values = {
        item01 = "Excalibur",
        item05 = "Aegis Shield",
    },
    Default = "item01",
    Multi = true,
    Text = "A dictionary dropdown",
})

Options.MyDictionaryDropdown:OnChanged(function(Value)
    for Key in Value do
        print(Key, "->", Options.MyDictionaryDropdown.Values[Key])
    end
end)
```
